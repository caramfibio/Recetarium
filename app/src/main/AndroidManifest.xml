<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RECETARIUM2">
        <activity
            android:name=".formulario_verReceta"
            android:exported="false" />
        <activity
            android:name=".formulario_recetarium"
            android:exported="false" />
        <!-- Corregido: la clase se llama SeleccionarSemanaActivity -->
        <activity
            android:name=".SeleccionarSemanaActivity"
            android:exported="false" />
        <activity
            android:name=".visualizar_activity"
            android:exported="false" />
        <activity
            android:name=".lista_recetas_activity"
            android:exported="false" />
        <activity
            android:name=".dias_activity"
            android:exported="false" />
        <!-- Corregido: la clase se llama HistorialActivity -->
        <activity
            android:name=".HistorialActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>